document.addEventListener("DOMContentLoaded", function () {
  const productGalleryButton = document.getElementById("productGalleryButton");
  productGalleryButton.addEventListener("click", function () {
    // Open the product gallery modal or perform the desired action
    alert("Open product gallery");
  });
});